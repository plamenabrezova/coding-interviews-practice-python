## Heap

- Heap is a special case of balanced binary tree data structure. 
- The root-node key is compared with its children and arranged accordingly. 
- In the case where the value of the root node is less than or equal to either of its children - `MIN HEAP`.
- In the case where the value of the root node is greater than or equal to either of its children - `MAX HEAP`.

--- 